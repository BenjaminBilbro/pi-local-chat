[project]
name = "pi-chat"
version = "0.1.0"
description = "Sleek web UI for pi coding agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pdfplumber>=0.11.10",
    "python-multipart>=0.0.32",
    "uvicorn[standard]",
    "websockets",
]
